# AgenticRepo
The voice agent performs actions based on user input, while TAU-Bench validates whether those actions lead to the correct outcome.  This creates a complete system with:  interaction (voice agent) decision-making (rule + utility) evaluation (TAU-Bench)
